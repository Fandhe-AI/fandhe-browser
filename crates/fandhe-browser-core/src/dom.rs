//! dom: `parse` が構築した木構造の DOM 表現・走査 API を担うモジュール（空 skeleton）。
//!
//! `parse` モジュールの出力を保持し、`query` モジュールや上位 crate
//! （`fandhe-browser-ai` の簡約 DOM 生成等）から参照される想定（TASK-24（24.1）・
//! ビヘイビア `CORE-1`）。本 PR ではモジュールを公開するのみで型・関数は未実装
//! （REPAIR-3: 実装済みを装わない）。本実装は TASK-24.5（Issue #39）で行う。
