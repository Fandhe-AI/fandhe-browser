//! fetch: URL・ローカルファイルからの取得を担うモジュール（空 skeleton）。
//!
//! `cdp`（`Page.navigate` 等）や将来の CLI から呼ばれ、取得した生データを
//! `parse` モジュールへ渡す想定の下位レイヤー。本 PR（TASK-24（24.1）・
//! ビヘイビア `CORE-1`）ではモジュールを公開するのみで関数は未実装
//! （REPAIR-3: 実装済みを装わない）。本実装は TASK-24.2（Issue #36）で行う。
//!
//! 実装時の申し送り:
//! - 取得先 URL の scheme（`file:` 等）・内部アドレスへの無検証アクセスを
//!   避ける（security.md「SSRF」）。
//! - 巨大レスポンス・無限リダイレクトに対する上限検証をアロケーション前に行う
//!   （security.md「リソース上限」）。
//! - エラーは [`crate::Error`] へ変換して返し、`unwrap`/`expect`/添字アクセス
//!   を外部入力の経路で使わない（coding-rust.md「エラーハンドリング」）。
